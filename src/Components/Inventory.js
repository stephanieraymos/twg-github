import React from "react";

const Inventory = () => {
  document.title = "Inventory";

  return (
    <>
      <h1>Inventory</h1>
      <button>Add Inventory</button>
    </>
  );
};

export default Inventory;
